# app.yaml

runtime: python311
env: standard
entrypoint: gunicorn -b :$PORT core.wsgi:application

handlers:
- url: /.*
  script: auto

runtime_config:
  python_version: 3

env_variables:
  APPENGINE_URL: https://to-do-web-app-409306.uc.r.appspot.com
